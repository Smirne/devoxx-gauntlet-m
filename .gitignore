node_modules/
dist/
dist-*/
.DS_Store
*.local
.vite/
tools/progress/shots/*.tmp.png

# agent worktrees (isolated subagent checkouts, never part of the project)
.claude/worktrees/

# stray agent scratch: briefs name a scratchpad/<tag>/ directory and an agent
# occasionally resolves that against the repo root instead of the session's own
# scratchpad. Never part of the project.
scratchpad/

# throwaway probes written into tests/ so they can run under vitest; excluded
# from the suite in vitest.config.ts for the same reason
tests/_*.test.ts
tests/zz-*.test.ts
tests/probe-*.test.ts
